# InkSplash Android 测试版使用说明

这是 InkSplash Android 内部联调测试包，用于测试 ESP32 电子纸设备的配网、账号绑定、图片上传和下发流程。

## 安装方式

1. 下载本 Release 中的 APK 文件。
2. 在 Android 手机上打开 APK。
3. 如果系统提示“不允许安装未知来源应用”，请按提示允许当前浏览器或文件管理器安装应用。
4. 安装完成后打开 `InkSplash`。

## 默认服务器

App 默认连接服务器：

```text
http://47.113.120.232
```

如果服务器地址有变化，可以在 App 首页顶部的 `Server base URL` 中手动修改。

## 注册和登录

1. 打开 App 后进入 `Account` 页面。
2. 输入邮箱和密码。
3. 首次使用点击 `Register` 注册。
4. 已注册账号点击 `Login` 登录。

注意：

- 密码至少 8 位。
- 当前测试版注册暂不需要邮箱验证码。
- 如果忘记密码，当前版本暂未提供找回密码功能。

## 添加设备

添加设备需要 ESP32 已经烧录支持 BLE 配网的固件，并且设备二维码中包含以下信息：

```json
{
  "ver": "v1",
  "name": "PROV_123456",
  "transport": "ble",
  "security": 1,
  "pop": "abcd1234",
  "device_id": "device001",
  "claim_code": "one-time-claim-code"
}
```

操作步骤：

1. 进入 `Bind` 页面。
2. 点击 `Scan device QR` 扫描设备二维码。
3. 点击 `Search BLE device` 搜索设备。
4. 选择扫描到的 BLE 设备并连接。
5. 选择需要连接的 Wi-Fi。
6. 输入 Wi-Fi 密码。
7. 点击 `Provision Wi-Fi and bind account`。
8. 配网成功后，设备会绑定到当前账号。

注意：

- 绑定码 `claim_code` 是一次性的。
- 如果设备已经被其他账号绑定，需要先在服务端解绑或重新生成绑定码。
- BLE 配网需要手机开启蓝牙和相关权限。

## 查看设备

进入 `Devices` 页面可以查看当前账号已绑定的设备。

设备信息包括：

- 设备号
- 当前图片版本
- 最近状态
- 电量
- Wi-Fi 信号强度

如果设备没有显示，请点击右上角刷新按钮。

## 上传并下发图片

1. 进入 `Upload` 页面。
2. 选择目标设备。
3. 选择图片方向：
   - `Auto`：自动判断横屏或竖屏
   - `Landscape`：横屏 800x480
   - `Portrait`：竖屏 480x800
4. 选择适配方式：
   - `Scale`：铺满并居中裁切
   - `Cut`：完整显示并补白
5. 根据需要开启或关闭 `Dither`。
6. 点击 `Choose image, upload, and assign`。
7. 从手机相册选择图片。
8. App 会上传图片，服务器处理完成后自动下发到所选设备。

上传成功后页面会显示：

- Image ID
- 图片尺寸
- 数据大小
- 格式
- SHA-256
- 处理后的预览图

## ESP32 显示流程

图片下发后，ESP32 需要按固件逻辑主动轮询服务器：

```text
GET /api/devices/{device_id}/current
```

如果版本号变化，ESP32 会下载新图片数据并刷新屏幕。

当前图片数据格式：

```text
epd4bit-indexed-v1
```

对于 800x480 或 480x800 图片，数据大小应为：

```text
192000 bytes
```

## 常见问题

### 1. Register failed

请检查：

- 邮箱是否为空
- 密码是否至少 8 位
- 当前邮箱是否已经注册过

### 2. Login failed

请检查：

- 邮箱是否正确
- 密码是否正确
- 服务器地址是否正确

### 3. 点击上传按钮后没有下发

请确认：

- 已经登录账号
- 已经绑定设备
- 已经在 `Upload` 页面选择目标设备
- 手机网络可以访问服务器
- 服务器地址是 `http://47.113.120.232`

### 4. 找不到 BLE 设备

请确认：

- ESP32 已进入 BLE 配网模式
- 手机蓝牙已开启
- App 已获得蓝牙权限
- 二维码中的 `name` 和设备广播名一致
- 设备没有被其他手机占用连接

### 5. 配网失败

请确认：

- Wi-Fi 名称和密码正确
- ESP32 支持当前 Wi-Fi 频段
- 路由器信号稳定
- 设备固件已正确配置 `device_id`、`device_token` 和服务器地址

### 6. 设备绑定失败

可能原因：

- `claim_code` 错误
- `claim_code` 已经使用过
- 设备已经被其他账号绑定
- 服务端还没有创建设备记录

需要管理员在服务端重新创建设备或重置设备绑定码。

## 当前测试版限制

- 暂不支持邮箱验证码。
- 暂不支持找回密码。
- 暂不支持多用户共享设备。
- 暂不支持图片历史列表。
- 暂不支持删除已上传图片。
- iOS 暂未提供可直接安装包，当前 Release 仅包含 Android APK。

## 反馈问题时请提供

请尽量提供以下信息，方便定位问题：

- 手机型号和 Android 版本
- App 操作步骤
- 报错截图
- 使用的设备号 `device_id`
- 是否已经完成账号登录
- 是否已经完成设备绑定
- ESP32 串口日志
